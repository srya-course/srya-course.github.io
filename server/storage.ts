// Server storage removed. This file intentionally left as a stub to avoid
// TypeScript import errors in older code. The project now uses
// `scripts/generate-content.js` to produce static JSON consumed by the client.

export default {};
